
# Cloudflare Pages — https://developers.cloudflare.com/pages/configuration/redirects/
#
# 루트(/) 리다이렉트는 호스트별로 다름(ms12 / mslms·mindstory / mindstory-lms) — 전역 `/` → `/app` 를 두면
# mslms·mindstory.kr 도 /app 으로만 가서 유아숲·회의 앱이 섞인다. 루트는 Worker `src/index.tsx` GET / 만 사용.
#
# www→apex 등은 Worker(oauth-public)에서 처리.
